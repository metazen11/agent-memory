{
  "mcpServers": {
    "agent-memory": {
      "type": "stdio",
      "command": ".venv/bin/python",
      "args": ["mcp_server.py"],
      "_comment": "install.js registers this with absolute paths in ~/.claude/.mcp.json"
    }
  }
}
